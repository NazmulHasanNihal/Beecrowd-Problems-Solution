let n = read_int () in
print_endline ("N[0] = " ^ string_of_int n);

  let n = n * 2 in
  print_endline ("N[1] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[2] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[3] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[4] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[5] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[6] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[7] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[8] = " ^ string_of_int n);
  let n = n * 2 in
  print_endline ("N[9] = " ^ string_of_int n);

